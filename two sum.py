class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        k = []

        for i in len(nums):
            req = target - i
        if req in nums:
            return k=req[i],n
        return k

        